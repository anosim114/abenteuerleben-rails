<%= render @campyear %>

<div>
  <% @camps.each do |camp| %>
    <br />
    <br />
    <%# todo: link with a button to create new camp for this campyear %>
    <%= link_to("Camp: " + camp.name,  camp_path(camp)) %>
    <%= render camp %>
  <% end %>
</div>

<div>
  <br>
  <%= link_to "Erstellen eines Camps", new_campyear_camp_path(@campyear) %>
  <br>
  <br>

  <%= link_to "Ändern des Campjahrs", edit_campyear_path(@campyear) %> |
  <%= link_to "Zurück zu allen Campjahren", campyears_path %> |

  <%= render "formelements/delete_action", url: @campyear %>
</div>
