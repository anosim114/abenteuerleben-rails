<% if record.errors[field].any? %>
  <div style="color: var(--red-100)">
    <ul>
      <% record.errors[field].each do |error| %>
        <li><i class="fa-solid fa-circle-exclamation" style="color: var(--danger)"></i> <%= error %></li>
      <% end %>
    </ul>
    </p>
  </div>
<% end %>
